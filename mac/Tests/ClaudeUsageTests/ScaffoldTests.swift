import XCTest

/// Placeholder so the test target has a source file and the scheme's test action
/// is wired up from Task 1. Replaced by real tests starting in Task 2.
final class ScaffoldTests: XCTestCase {
    func testTestTargetRuns() {
        XCTAssertTrue(true, "test target builds and executes")
    }
}
